node_modules/
target/
dist/
data/

# shadcn-svelte components are regenerated by `just install` (see .justfile)
# from the list passed to `shadcn-svelte add`. Only `lib/utils.ts` and
# `components.json` are committed.
frontend/3rdparty/shadcn/
