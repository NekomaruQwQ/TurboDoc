# `shadcn-svelte` components are regenerated by `just install` (see .justfile)
# from the list passed to `shadcn-svelte add`.
#
# Note that `shadcn/utils.ts` is still tracked as `shadcn-svelte add` does not
# generate it.
shadcn/components/
